from django.db import models

# Create your models here.
class servicio (models.models)
    id = models.texfield("id")
    nombre = models.texfield("nombre")

    def_str_(Self):
        return f'{Self.servicio}'
